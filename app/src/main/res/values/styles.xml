<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="PrimaryFlatButton" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/popColor</item>
        <item name="colorControlHighlight">@color/textColor</item>
        <item name="colorAccent">@color/popColor</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <style name="SecondaryFlatButton" parent="Widget.AppCompat.Button">
    <item name="colorButtonNormal">@color/secondaryPopColor</item>
    <item name="colorControlHighlight">#000000</item>
    <item name="colorAccent">#000000</item>
    <item name="android:textColor">#FFFFFF</item>
    </style>
    <style name="deleteButton" parent="Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="colorControlHighlight">#000000</item>
        <item name="colorAccent">#000000</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>
    <style name="menu_text_style" parent="@android:style/TextAppearance.DeviceDefault.Widget.ActionBar.Menu">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#555555</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <style name="moving_text_view">
        <item name="android:singleLine">true</item>
        <item name="android:ellipsize">marquee</item>
        <item name="android:marqueeRepeatLimit">marquee_forever</item>
        <item name="android:focusable">true</item>
        <item name="android:focusableInTouchMode">true</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>

    </style>

</resources>